name: test

exports:
  - ecsact_execute_systems

sources:
  - ecsact_build_test.cc
  - Makefile
  - codegen:
      - cpp_header
    outdir: include
  - fetch: https://some/url/yo
    strip_prefix: some-pkg/include
    outdir: include

compile:
  steps:
  - make all
